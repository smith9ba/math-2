#!/usr/bin/env node

// get argument values
var	op   =  process.argv[2], // the operator for the math
	num1 = +process.argv[3], // the first operand
	num2 = +process.argv[4], // the second operand
	math = require('./math');// import our math module

console.log(math[op](num1, num2));